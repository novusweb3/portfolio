<style is:global>
  /* Reset all backgrounds first */
  html, 
  body, 
  .main-content,
  .main-content::before,
  section,
  div {
    background: none;
    background-image: none;
    background-color: #1a1a1a;
  }

  html {
    font-family: system-ui, sans-serif;
    color-scheme: dark;
  }

  body {
    margin: 0;
    min-height: 100vh;
    display: flex;
  }

  .main-content {
    margin-left: 280px;
    width: calc(100% - 280px);
    min-height: 100vh;
    position: relative;
  }

  /* Remove the before element completely */
  .main-content::before {
    display: none;
  }
</style> 