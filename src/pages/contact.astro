---
import MainLayout from '../layouts/MainLayout.astro';
---

<MainLayout title="Contact - Adrian Birsan" description="Get in touch for collaborations and inquiries">
  <main class="contact-container">
    <div class="contact-content">
      <section class="contact-message">
        <h1>Let's Connect</h1>
        
        <div class="intro-text">
          <p>Hey there! You're just a message away from turning your digital dreams into reality.</p>
          
          <p>Remember that project you've been putting off? Or that brilliant idea you've been too hesitant to start? Well, now's your chance to kick-start it! Let's partner up and make sure by the end of this year, you're not just setting resolutions but celebrating milestones.</p>
          
          <p class="highlight">Why wait? Let's make 2024 the year you say, "I did it!"</p>
          
          <ul class="services-list">
            <li>Got a vision for your business growth? Let's strategize with AI and digital marketing.</li>
            <li>Need a custom solution? Whether it's a bot or a bespoke software, I've got you covered.</li>
            <li>Your website could be your next big success story.</li>
          </ul>
          
          <p>Start today, and by December 31st, you won't just be setting resolutions; you'll be celebrating achievements!</p>
          
          <p> Drop me a line, and let's get started on making your project a reality.</p>
        </div>
      </section>

      <section class="contact-form-section">
        <form id="contactForm" class="contact-form">
          <div class="form-group">
            <label for="from_name">Name</label>
            <input type="text" id="from_name" name="from_name" required />
          </div>
          
          <div class="form-group">
            <label for="to_name">To</label>
            <input type="text" id="to_name" name="to_name" value="Adrian" readonly hidden />
          </div>
          
          <div class="form-group">
            <label for="message">Message</label>
            <textarea id="message" name="message" rows="5" required></textarea>
          </div>
          
          <button type="submit">Send Message</button>
        </form>
      </section>
    </div>
  </main>
</MainLayout>

<style>
.contact-container {
  padding: 2rem;
  max-width: 1200px;
  margin: 0 auto;
}

.contact-content {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 4rem;
  align-items: start;
}

h1 {
  font-size: 3rem;
  color: #ff8c00;
  margin-bottom: 2rem;
}

.intro-text {
  color: #e0e0e0;
  line-height: 1.6;
}

.intro-text p {
  margin-bottom: 1.5rem;
}

.highlight {
  color: #ff8c00;
  font-size: 1.2rem;
  font-weight: 500;
}

.services-list {
  list-style: none;
  padding: 0;
  margin: 2rem 0;
}

.services-list li {
  margin-bottom: 1rem;
  padding-left: 1.5rem;
  position: relative;
}

.services-list li:before {
  content: "→";
  position: absolute;
  left: 0;
  color: #ff8c00;
}

.contact-form {
  background: #1a1a1a;
  padding: 2rem;
  border-radius: 12px;
  border: 1px solid #333;
}

.form-group {
  margin-bottom: 1.5rem;
}

label {
  display: block;
  margin-bottom: 0.5rem;
  color: #e0e0e0;
}

input,
textarea {
  width: 100%;
  padding: 0.75rem;
  background: #242424;
  border: 1px solid #333;
  border-radius: 4px;
  color: #e0e0e0;
  font-size: 1rem;
}

input:focus,
textarea:focus {
  outline: none;
  border-color: #ff8c00;
}

button {
  background: #ff8c00;
  color: white;
  padding: 1rem 2rem;
  border: none;
  border-radius: 4px;
  font-size: 1rem;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

button:hover {
  background: #ff9f2b;
}

@media (max-width: 768px) {
  .contact-content {
    grid-template-columns: 1fr;
    gap: 2rem;
  }
  
  h1 {
    font-size: 2.5rem;
  }
}

.success-message {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background: #1a1a1a;
  padding: 2rem;
  border-radius: 12px;
  text-align: center;
  box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
  display: none;
  z-index: 1000;
}

.success-message.show {
  display: block;
  animation: fadeIn 0.5s ease;
}

.success-message h3 {
  color: #4CAF50;
  margin: 1rem 0;
  font-size: 1.5rem;
}

.success-message p {
  color: #e0e0e0;
}

.success-icon {
  width: 80px;
  height: 80px;
  margin: 0 auto;
}

.checkmark {
  width: 80px;
  height: 80px;
  border-radius: 50%;
  display: block;
  stroke-width: 2;
  stroke: #4CAF50;
  stroke-miterlimit: 10;
  box-shadow: inset 0 0 0 #4CAF50;
  animation: fill .4s ease-in-out .4s forwards, scale .3s ease-in-out .9s both;
}

.checkmark__circle {
  stroke-dasharray: 166;
  stroke-dashoffset: 166;
  stroke-width: 2;
  stroke-miterlimit: 10;
  stroke: #4CAF50;
  fill: none;
  animation: stroke 0.6s cubic-bezier(0.65, 0, 0.45, 1) forwards;
}

.checkmark__check {
  transform-origin: 50% 50%;
  stroke-dasharray: 48;
  stroke-dashoffset: 48;
  animation: stroke 0.3s cubic-bezier(0.65, 0, 0.45, 1) 0.8s forwards;
}

@keyframes stroke {
  100% {
    stroke-dashoffset: 0;
  }
}

@keyframes scale {
  0%, 100% {
    transform: none;
  }
  50% {
    transform: scale3d(1.1, 1.1, 1);
  }
}

@keyframes fill {
  100% {
    box-shadow: inset 0 0 0 80px #4CAF50;
  }
}

@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translate(-50%, -40%);
  }
  to {
    opacity: 1;
    transform: translate(-50%, -50%);
  }
}

.overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.7);
  display: none;
  z-index: 999;
}

.overlay.show {
  display: block;
  animation: fadeIn 0.3s ease;
}

.error-message {
  color: #ff4444;
  margin-top: 1rem;
  padding: 0.75rem;
  border-radius: 4px;
  background: rgba(255, 68, 68, 0.1);
  text-align: center;
  animation: fadeIn 0.3s ease;
}
</style>

<script>
import emailjs from '@emailjs/browser';

// Initialize EmailJS
emailjs.init(import.meta.env.PUBLIC_EMAILJS_PUBLIC_KEY);

document.getElementById('contactForm').addEventListener('submit', async (e) => {
  e.preventDefault();
  
  const form = e.target as HTMLFormElement;
  const submitButton = form.querySelector('button') as HTMLButtonElement;
  
  submitButton.disabled = true;
  submitButton.textContent = 'Sending...';

  try {
    const result = await emailjs.sendForm(
      import.meta.env.PUBLIC_EMAILJS_SERVICE_ID,
      import.meta.env.PUBLIC_EMAILJS_TEMPLATE_ID,
      form,
      import.meta.env.PUBLIC_EMAILJS_PUBLIC_KEY
    );

    if (result.status === 200) {
      submitButton.textContent = 'Message Sent';
      form.reset();
      // Reset the hidden "to_name" field
      const toNameInput = form.querySelector('[name="to_name"]') as HTMLInputElement;
      if (toNameInput) toNameInput.value = 'Adrian';
    }

  } catch (error) {
    console.error('Error:', error);
    submitButton.textContent = 'Error Sending';
  } finally {
    setTimeout(() => {
      submitButton.disabled = false;
      submitButton.textContent = 'Send Message';
    }, 3000);
  }
});
</script>
